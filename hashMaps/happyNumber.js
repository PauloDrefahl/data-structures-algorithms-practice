/**
 * @param {number} n
 * @return {boolean}
 */
var isHappy = function(n) {
    number = []
    for (let i = 0; i < n; n){
        number.push(num)
    }
    
    sum = 0

    while (sum != 1){
        number.forEach( (num) => num ** 2 )
        console.log(number)
        sum = 1
    }
    
    return true;
};


